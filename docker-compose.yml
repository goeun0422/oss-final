version: '3.8'

services:
  backend:
    build: ./back
    ports:
      - "8000:8000"
    container_name: fastapi_back

  frontend:
    build: ./front
    ports:
      - "8501:8501"
    container_name: streamlit_front
    depends_on:
      - backend